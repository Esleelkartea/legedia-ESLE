<?php

/**
 * Base class that represents a row from the 'item' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Wed Oct 27 21:17:36 2010
 *
 * @package    lib.model.om
 */
abstract class BaseItem extends BaseObject  implements Persistent {


  const PEER = 'ItemPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ItemPeer
	 */
	protected static $peer;

	/**
	 * The value for the id_item field.
	 * @var        int
	 */
	protected $id_item;

	/**
	 * The value for the id_formulario field.
	 * @var        int
	 */
	protected $id_formulario;

	/**
	 * The value for the id_item_base field.
	 * @var        int
	 */
	protected $id_item_base;

	/**
	 * The value for the texto_corto field.
	 * @var        string
	 */
	protected $texto_corto;

	/**
	 * The value for the texto_largo field.
	 * @var        string
	 */
	protected $texto_largo;

	/**
	 * The value for the si_no field.
	 * @var        boolean
	 */
	protected $si_no;

	/**
	 * The value for the texto_auxiliar field.
	 * @var        string
	 */
	protected $texto_auxiliar;

	/**
	 * The value for the fecha field.
	 * @var        string
	 */
	protected $fecha;

	/**
	 * The value for the numero field.
	 * @var        int
	 */
	protected $numero;

	/**
	 * The value for the id_tabla field.
	 * @var        int
	 */
	protected $id_tabla;

	/**
	 * The value for the id_objeto field.
	 * @var        int
	 */
	protected $id_objeto;

	/**
	 * The value for the anio field.
	 * @var        int
	 */
	protected $anio;

	/**
	 * @var        ItemBase
	 */
	protected $aItemBase;

	/**
	 * @var        Formulario
	 */
	protected $aFormulario;

	/**
	 * @var        array Documento[] Collection to store aggregation of Documento objects.
	 */
	protected $collDocumentos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDocumentos.
	 */
	private $lastDocumentoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseItem object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [id_item] column value.
	 * 
	 * @return     int
	 */
	public function getIdItem()
	{
		return $this->id_item;
	}

	/**
	 * Get the [id_formulario] column value.
	 * 
	 * @return     int
	 */
	public function getIdFormulario()
	{
		return $this->id_formulario;
	}

	/**
	 * Get the [id_item_base] column value.
	 * 
	 * @return     int
	 */
	public function getIdItemBase()
	{
		return $this->id_item_base;
	}

	/**
	 * Get the [texto_corto] column value.
	 * 
	 * @return     string
	 */
	public function getTextoCorto()
	{
		return $this->texto_corto;
	}

	/**
	 * Get the [texto_largo] column value.
	 * 
	 * @return     string
	 */
	public function getTextoLargo()
	{
		return $this->texto_largo;
	}

	/**
	 * Get the [si_no] column value.
	 * 
	 * @return     boolean
	 */
	public function getSiNo()
	{
		return $this->si_no;
	}

	/**
	 * Get the [texto_auxiliar] column value.
	 * 
	 * @return     string
	 */
	public function getTextoAuxiliar()
	{
		return $this->texto_auxiliar;
	}

	/**
	 * Get the [optionally formatted] temporal [fecha] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFecha($format = 'Y-m-d H:i:s')
	{
		if ($this->fecha === null) {
			return null;
		}


		if ($this->fecha === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fecha);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [numero] column value.
	 * 
	 * @return     int
	 */
	public function getNumero()
	{
		return $this->numero;
	}

	/**
	 * Get the [id_tabla] column value.
	 * 
	 * @return     int
	 */
	public function getIdTabla()
	{
		return $this->id_tabla;
	}

	/**
	 * Get the [id_objeto] column value.
	 * 
	 * @return     int
	 */
	public function getIdObjeto()
	{
		return $this->id_objeto;
	}

	/**
	 * Get the [anio] column value.
	 * 
	 * @return     int
	 */
	public function getAnio()
	{
		return $this->anio;
	}

	/**
	 * Set the value of [id_item] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setIdItem($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_item !== $v) {
			$this->id_item = $v;
			$this->modifiedColumns[] = ItemPeer::ID_ITEM;
		}

		return $this;
	} // setIdItem()

	/**
	 * Set the value of [id_formulario] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setIdFormulario($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_formulario !== $v) {
			$this->id_formulario = $v;
			$this->modifiedColumns[] = ItemPeer::ID_FORMULARIO;
		}

		if ($this->aFormulario !== null && $this->aFormulario->getIdFormulario() !== $v) {
			$this->aFormulario = null;
		}

		return $this;
	} // setIdFormulario()

	/**
	 * Set the value of [id_item_base] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setIdItemBase($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_item_base !== $v) {
			$this->id_item_base = $v;
			$this->modifiedColumns[] = ItemPeer::ID_ITEM_BASE;
		}

		if ($this->aItemBase !== null && $this->aItemBase->getIdItemBase() !== $v) {
			$this->aItemBase = null;
		}

		return $this;
	} // setIdItemBase()

	/**
	 * Set the value of [texto_corto] column.
	 * 
	 * @param      string $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setTextoCorto($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->texto_corto !== $v) {
			$this->texto_corto = $v;
			$this->modifiedColumns[] = ItemPeer::TEXTO_CORTO;
		}

		return $this;
	} // setTextoCorto()

	/**
	 * Set the value of [texto_largo] column.
	 * 
	 * @param      string $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setTextoLargo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->texto_largo !== $v) {
			$this->texto_largo = $v;
			$this->modifiedColumns[] = ItemPeer::TEXTO_LARGO;
		}

		return $this;
	} // setTextoLargo()

	/**
	 * Set the value of [si_no] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setSiNo($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->si_no !== $v) {
			$this->si_no = $v;
			$this->modifiedColumns[] = ItemPeer::SI_NO;
		}

		return $this;
	} // setSiNo()

	/**
	 * Set the value of [texto_auxiliar] column.
	 * 
	 * @param      string $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setTextoAuxiliar($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->texto_auxiliar !== $v) {
			$this->texto_auxiliar = $v;
			$this->modifiedColumns[] = ItemPeer::TEXTO_AUXILIAR;
		}

		return $this;
	} // setTextoAuxiliar()

	/**
	 * Sets the value of [fecha] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Item The current object (for fluent API support)
	 */
	public function setFecha($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha !== null && $tmpDt = new DateTime($this->fecha)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fecha = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ItemPeer::FECHA;
			}
		} // if either are not null

		return $this;
	} // setFecha()

	/**
	 * Set the value of [numero] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setNumero($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->numero !== $v) {
			$this->numero = $v;
			$this->modifiedColumns[] = ItemPeer::NUMERO;
		}

		return $this;
	} // setNumero()

	/**
	 * Set the value of [id_tabla] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setIdTabla($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_tabla !== $v) {
			$this->id_tabla = $v;
			$this->modifiedColumns[] = ItemPeer::ID_TABLA;
		}

		return $this;
	} // setIdTabla()

	/**
	 * Set the value of [id_objeto] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setIdObjeto($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_objeto !== $v) {
			$this->id_objeto = $v;
			$this->modifiedColumns[] = ItemPeer::ID_OBJETO;
		}

		return $this;
	} // setIdObjeto()

	/**
	 * Set the value of [anio] column.
	 * 
	 * @param      int $v new value
	 * @return     Item The current object (for fluent API support)
	 */
	public function setAnio($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->anio !== $v) {
			$this->anio = $v;
			$this->modifiedColumns[] = ItemPeer::ANIO;
		}

		return $this;
	} // setAnio()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id_item = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_formulario = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->id_item_base = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->texto_corto = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->texto_largo = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->si_no = ($row[$startcol + 5] !== null) ? (boolean) $row[$startcol + 5] : null;
			$this->texto_auxiliar = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->fecha = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->numero = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->id_tabla = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->id_objeto = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->anio = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 12; // 12 = ItemPeer::NUM_COLUMNS - ItemPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Item object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aFormulario !== null && $this->id_formulario !== $this->aFormulario->getIdFormulario()) {
			$this->aFormulario = null;
		}
		if ($this->aItemBase !== null && $this->id_item_base !== $this->aItemBase->getIdItemBase()) {
			$this->aItemBase = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ItemPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ItemPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aItemBase = null;
			$this->aFormulario = null;
			$this->collDocumentos = null;
			$this->lastDocumentoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseItem:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ItemPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			ItemPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseItem:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseItem:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ItemPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseItem:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			ItemPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aItemBase !== null) {
				if ($this->aItemBase->isModified() || $this->aItemBase->isNew()) {
					$affectedRows += $this->aItemBase->save($con);
				}
				$this->setItemBase($this->aItemBase);
			}

			if ($this->aFormulario !== null) {
				if ($this->aFormulario->isModified() || $this->aFormulario->isNew()) {
					$affectedRows += $this->aFormulario->save($con);
				}
				$this->setFormulario($this->aFormulario);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = ItemPeer::ID_ITEM;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ItemPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setIdItem($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ItemPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collDocumentos !== null) {
				foreach ($this->collDocumentos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aItemBase !== null) {
				if (!$this->aItemBase->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aItemBase->getValidationFailures());
				}
			}

			if ($this->aFormulario !== null) {
				if (!$this->aFormulario->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aFormulario->getValidationFailures());
				}
			}


			if (($retval = ItemPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collDocumentos !== null) {
					foreach ($this->collDocumentos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ItemPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getIdItem();
				break;
			case 1:
				return $this->getIdFormulario();
				break;
			case 2:
				return $this->getIdItemBase();
				break;
			case 3:
				return $this->getTextoCorto();
				break;
			case 4:
				return $this->getTextoLargo();
				break;
			case 5:
				return $this->getSiNo();
				break;
			case 6:
				return $this->getTextoAuxiliar();
				break;
			case 7:
				return $this->getFecha();
				break;
			case 8:
				return $this->getNumero();
				break;
			case 9:
				return $this->getIdTabla();
				break;
			case 10:
				return $this->getIdObjeto();
				break;
			case 11:
				return $this->getAnio();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ItemPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getIdItem(),
			$keys[1] => $this->getIdFormulario(),
			$keys[2] => $this->getIdItemBase(),
			$keys[3] => $this->getTextoCorto(),
			$keys[4] => $this->getTextoLargo(),
			$keys[5] => $this->getSiNo(),
			$keys[6] => $this->getTextoAuxiliar(),
			$keys[7] => $this->getFecha(),
			$keys[8] => $this->getNumero(),
			$keys[9] => $this->getIdTabla(),
			$keys[10] => $this->getIdObjeto(),
			$keys[11] => $this->getAnio(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ItemPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setIdItem($value);
				break;
			case 1:
				$this->setIdFormulario($value);
				break;
			case 2:
				$this->setIdItemBase($value);
				break;
			case 3:
				$this->setTextoCorto($value);
				break;
			case 4:
				$this->setTextoLargo($value);
				break;
			case 5:
				$this->setSiNo($value);
				break;
			case 6:
				$this->setTextoAuxiliar($value);
				break;
			case 7:
				$this->setFecha($value);
				break;
			case 8:
				$this->setNumero($value);
				break;
			case 9:
				$this->setIdTabla($value);
				break;
			case 10:
				$this->setIdObjeto($value);
				break;
			case 11:
				$this->setAnio($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ItemPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setIdItem($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdFormulario($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setIdItemBase($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setTextoCorto($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setTextoLargo($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setSiNo($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setTextoAuxiliar($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setFecha($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setNumero($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setIdTabla($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setIdObjeto($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setAnio($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ItemPeer::DATABASE_NAME);

		if ($this->isColumnModified(ItemPeer::ID_ITEM)) $criteria->add(ItemPeer::ID_ITEM, $this->id_item);
		if ($this->isColumnModified(ItemPeer::ID_FORMULARIO)) $criteria->add(ItemPeer::ID_FORMULARIO, $this->id_formulario);
		if ($this->isColumnModified(ItemPeer::ID_ITEM_BASE)) $criteria->add(ItemPeer::ID_ITEM_BASE, $this->id_item_base);
		if ($this->isColumnModified(ItemPeer::TEXTO_CORTO)) $criteria->add(ItemPeer::TEXTO_CORTO, $this->texto_corto);
		if ($this->isColumnModified(ItemPeer::TEXTO_LARGO)) $criteria->add(ItemPeer::TEXTO_LARGO, $this->texto_largo);
		if ($this->isColumnModified(ItemPeer::SI_NO)) $criteria->add(ItemPeer::SI_NO, $this->si_no);
		if ($this->isColumnModified(ItemPeer::TEXTO_AUXILIAR)) $criteria->add(ItemPeer::TEXTO_AUXILIAR, $this->texto_auxiliar);
		if ($this->isColumnModified(ItemPeer::FECHA)) $criteria->add(ItemPeer::FECHA, $this->fecha);
		if ($this->isColumnModified(ItemPeer::NUMERO)) $criteria->add(ItemPeer::NUMERO, $this->numero);
		if ($this->isColumnModified(ItemPeer::ID_TABLA)) $criteria->add(ItemPeer::ID_TABLA, $this->id_tabla);
		if ($this->isColumnModified(ItemPeer::ID_OBJETO)) $criteria->add(ItemPeer::ID_OBJETO, $this->id_objeto);
		if ($this->isColumnModified(ItemPeer::ANIO)) $criteria->add(ItemPeer::ANIO, $this->anio);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ItemPeer::DATABASE_NAME);

		$criteria->add(ItemPeer::ID_ITEM, $this->id_item);
		$criteria->add(ItemPeer::ID_FORMULARIO, $this->id_formulario);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getIdItem();

		$pks[1] = $this->getIdFormulario();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setIdItem($keys[0]);

		$this->setIdFormulario($keys[1]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Item (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdFormulario($this->id_formulario);

		$copyObj->setIdItemBase($this->id_item_base);

		$copyObj->setTextoCorto($this->texto_corto);

		$copyObj->setTextoLargo($this->texto_largo);

		$copyObj->setSiNo($this->si_no);

		$copyObj->setTextoAuxiliar($this->texto_auxiliar);

		$copyObj->setFecha($this->fecha);

		$copyObj->setNumero($this->numero);

		$copyObj->setIdTabla($this->id_tabla);

		$copyObj->setIdObjeto($this->id_objeto);

		$copyObj->setAnio($this->anio);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getDocumentos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDocumento($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setIdItem(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Item Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ItemPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ItemPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a ItemBase object.
	 *
	 * @param      ItemBase $v
	 * @return     Item The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setItemBase(ItemBase $v = null)
	{
		if ($v === null) {
			$this->setIdItemBase(NULL);
		} else {
			$this->setIdItemBase($v->getIdItemBase());
		}

		$this->aItemBase = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the ItemBase object, it will not be re-added.
		if ($v !== null) {
			$v->addItem($this);
		}

		return $this;
	}


	/**
	 * Get the associated ItemBase object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     ItemBase The associated ItemBase object.
	 * @throws     PropelException
	 */
	public function getItemBase(PropelPDO $con = null)
	{
		if ($this->aItemBase === null && ($this->id_item_base !== null)) {
			$c = new Criteria(ItemBasePeer::DATABASE_NAME);
			$c->add(ItemBasePeer::ID_ITEM_BASE, $this->id_item_base);
			$this->aItemBase = ItemBasePeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aItemBase->addItems($this);
			 */
		}
		return $this->aItemBase;
	}

	/**
	 * Declares an association between this object and a Formulario object.
	 *
	 * @param      Formulario $v
	 * @return     Item The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setFormulario(Formulario $v = null)
	{
		if ($v === null) {
			$this->setIdFormulario(NULL);
		} else {
			$this->setIdFormulario($v->getIdFormulario());
		}

		$this->aFormulario = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Formulario object, it will not be re-added.
		if ($v !== null) {
			$v->addItem($this);
		}

		return $this;
	}


	/**
	 * Get the associated Formulario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Formulario The associated Formulario object.
	 * @throws     PropelException
	 */
	public function getFormulario(PropelPDO $con = null)
	{
		if ($this->aFormulario === null && ($this->id_formulario !== null)) {
			$c = new Criteria(FormularioPeer::DATABASE_NAME);
			$c->add(FormularioPeer::ID_FORMULARIO, $this->id_formulario);
			$this->aFormulario = FormularioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aFormulario->addItems($this);
			 */
		}
		return $this->aFormulario;
	}

	/**
	 * Clears out the collDocumentos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDocumentos()
	 */
	public function clearDocumentos()
	{
		$this->collDocumentos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDocumentos collection (array).
	 *
	 * By default this just sets the collDocumentos collection to an empty array (like clearcollDocumentos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDocumentos()
	{
		$this->collDocumentos = array();
	}

	/**
	 * Gets an array of Documento objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Item has previously been saved, it will retrieve
	 * related Documentos from storage. If this Item is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Documento[]
	 * @throws     PropelException
	 */
	public function getDocumentos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ItemPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumentos === null) {
			if ($this->isNew()) {
			   $this->collDocumentos = array();
			} else {

				$criteria->add(DocumentoPeer::ID_ITEM, $this->id_item);

				DocumentoPeer::addSelectColumns($criteria);
				$this->collDocumentos = DocumentoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DocumentoPeer::ID_ITEM, $this->id_item);

				DocumentoPeer::addSelectColumns($criteria);
				if (!isset($this->lastDocumentoCriteria) || !$this->lastDocumentoCriteria->equals($criteria)) {
					$this->collDocumentos = DocumentoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDocumentoCriteria = $criteria;
		return $this->collDocumentos;
	}

	/**
	 * Returns the number of related Documento objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Documento objects.
	 * @throws     PropelException
	 */
	public function countDocumentos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ItemPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDocumentos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DocumentoPeer::ID_ITEM, $this->id_item);

				$count = DocumentoPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DocumentoPeer::ID_ITEM, $this->id_item);

				if (!isset($this->lastDocumentoCriteria) || !$this->lastDocumentoCriteria->equals($criteria)) {
					$count = DocumentoPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collDocumentos);
				}
			} else {
				$count = count($this->collDocumentos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Documento object to this object
	 * through the Documento foreign key attribute.
	 *
	 * @param      Documento $l Documento
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDocumento(Documento $l)
	{
		if ($this->collDocumentos === null) {
			$this->initDocumentos();
		}
		if (!in_array($l, $this->collDocumentos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDocumentos, $l);
			$l->setItem($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Item is new, it will return
	 * an empty collection; or if this Item has previously
	 * been saved, it will retrieve related Documentos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Item.
	 */
	public function getDocumentosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ItemPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumentos === null) {
			if ($this->isNew()) {
				$this->collDocumentos = array();
			} else {

				$criteria->add(DocumentoPeer::ID_ITEM, $this->id_item);

				$this->collDocumentos = DocumentoPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DocumentoPeer::ID_ITEM, $this->id_item);

			if (!isset($this->lastDocumentoCriteria) || !$this->lastDocumentoCriteria->equals($criteria)) {
				$this->collDocumentos = DocumentoPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastDocumentoCriteria = $criteria;

		return $this->collDocumentos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collDocumentos) {
				foreach ((array) $this->collDocumentos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collDocumentos = null;
			$this->aItemBase = null;
			$this->aFormulario = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseItem:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseItem::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseItem
